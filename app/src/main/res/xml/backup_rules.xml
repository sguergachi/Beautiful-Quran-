<?xml version="1.0" encoding="utf-8"?>
<!-- Android 11 and below (android:fullBackupContent). The audio cache is
     rebuildable from the network and can reach ~1 GB, so keep it out of
     cloud backups. -->
<full-backup-content>
    <exclude domain="file" path="audio" />
</full-backup-content>
